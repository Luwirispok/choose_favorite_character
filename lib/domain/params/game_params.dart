import 'package:choose_favorite_character/core/enum/game_enum.dart';

class GameParams {
  final GameEnum gameEnum;
  final int countElements;

  GameParams({required this.gameEnum, required this.countElements});
}
